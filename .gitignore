node_modules/
dist/
.DS_Store
*.log
.claude/settings.local.json

# Generated by scripts/build-manual.mjs, which both `npm run dev` and
# `npm run build` invoke — so it is never needed from git, and committing it
# would churn ~860KB (mostly one embedded image) on every docs edit.
public/manual/
